<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Profile</title>

  <!-- * static files -->
  <link rel="stylesheet" type="text/css" href="./styles/user.css">
  <link rel="stylesheet" type="text/css" href="./styles/null.css">
  
  
  <!-- * bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>

  <nav class="bg-warning">
    <ul class="navbar-nav">
      <% if (message) { %>
      <li class="nav-item">
        <a class="nav-link" href="/">Home</a>
      </li>
      <li class="nav-item">
        <a class="nav-link active" aria-current="page" href="/user">Profile</a>
      </li>
      <% } else { %>
      <li class="nav-item">
        <a class="nav-link" href="/">Home</a>
      </li>
      <% } %>
    </ul>
  </nav>

  <main>
    <% if (message) { %>
    <div><h1><%= message %></h1></div>
    <div>
      <p><strong>Username:</strong> <%= data.username %></p>
      <p><strong>Email:</strong> <%= data.email %></p>
      <p><strong>Account created:</strong> <%= data.timestamp %></p>
    </div>
    <% } else { %>
    <h1 class="text-danger text-center mt-5">ERROR: 404 | USER NOT FOUND.</h1>
    <% } %>
  </main>
  
</body>
</html>
